class Aws {
    constructor(id, referencia, nome, id_user, data_criacao) {
        this.id = id;
        this.referencia = referencia;
        this.nome = nome;
        this.id_user = id_user;
        this.data_criacao = data_criacao;
    }
}
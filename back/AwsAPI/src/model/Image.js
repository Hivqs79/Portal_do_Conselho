class Aws {
    constructor(id, referencia, titulo, id_user, data_criacao) {
        this.id = id;
        this.referencia = referencia;
        this.titulo = titulo;
        this.id_user = id_user;
        this.data_criacao = data_criacao;
    }
}